Per il compositing, ci ho guardato ancora oggi. Ho visto che il comportamento strano su nord-europa sia cmq nei dati (a volte, ma non sempre, relazionato a BRDF). Per ora non vedo opzione di migliorarlo: se vogliamo continuare con LTDR, magari possiamo concentrarsi cmq su Africa e/o tropic: vediamo. Quando ho 3 anni processati ti invio un nuovo stack. Sono un pochino piu contento dei risultati ora. Adesso faccio cosi:
-	Only implement water mask from quality bits: if water, result = noData
-	Evaluate highest two NDVI values within compositing window. If only a single valid value exists, select that (flag=0), if we have at least two valid NDVI values, I check which of the two has the lowest vzen-value. However, in order for the second-highest NDVI value to be selected, it should also be 0.90* the value of the highest NDVI (to avoid much lower values in the composite). If first maximum is selected, flag=1, if second maximum is selected flag=2
-	If the selected value has BRDF-correction issues (bit 14) we add 4 to the flag (so could have values 4,5,6). The flag is written as the last digit (3425 would mean an NDVI of 0.342, a flag of 5, which is decomposed into 4 (BRDF-issues) and 1 (first maximum selected).
